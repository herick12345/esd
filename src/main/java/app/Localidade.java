package app;

// use esta classe para representar uma localidade retornada como resultado
// Lembre que um "record" é uma classe especial que já fornece construtor e getters
public record Localidade (String pais, String local) {
    // você pode acrescentar métodos para implementar novas operações (se necessárias !)
}
